// Q: Create an array with consta and try to reassigne the 
// arrag and obsorve the error

const arr = [1, 2, 3, 4, 5];
// assigning arr again
arr = []; // assigning is not allowed with Const

const num = 13;
num++; // this also throw error 
